export const SITE_NAME = "Universe Portfolio";

export const PAGINATION = {
  DEFAULT_PAGE_SIZE: 10,
  MAX_PAGE_SIZE: 50,
} as const;

export const POSTS = {
  MAX_TITLE_LENGTH: 200,
  MAX_EXCERPT_LENGTH: 500,
} as const;
